name: Deploy catalog-ms to AWS

on:
  push:
    branches:
      - main
    paths:
      - 'services/catalog-ms/**'
      - '.github/workflows/deploy-catalog.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      AWS_REGION: ap-south-1
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}

      - name: Deploy CloudFormation stack for catalog-ms
        run: |
          aws cloudformation deploy \
            --template-file infra/catalog-ms.cloudformation.yml \
            --stack-name catalog-ms-stack \
            --capabilities CAPABILITY_NAMED_IAM \
            --parameter-overrides EnvName=dev ImageUrl=${{ secrets.CATALOG_MS_IMAGE_URL }} DBUsername=${{ secrets.CATALOG_DB_USERNAME }} DBPassword=${{ secrets.CATALOG_DB_PASSWORD }}
